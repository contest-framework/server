{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FileConfiguration",
  "description": "configuration data for <https://github.com/contest-framework/server>",
  "type": "object",
  "required": [
    "actions"
  ],
  "properties": {
    "actions": {
      "description": "tell Contest how to test your files",
      "type": "array",
      "items": {
        "$ref": "#/definitions/FileAction"
      }
    },
    "options": {
      "description": "configure the layout and behavior",
      "anyOf": [
        {
          "$ref": "#/definitions/FileOptions"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "definitions": {
    "FileAction": {
      "description": "a particular test",
      "type": "object",
      "required": [
        "run",
        "type"
      ],
      "properties": {
        "files": {
          "description": "the files for which this command applies as a glob expression",
          "type": [
            "string",
            "null"
          ]
        },
        "run": {
          "description": "the command to run",
          "type": "string"
        },
        "type": {
          "type": "string"
        },
        "vars": {
          "description": "define additional variables to use in the \"run\" string",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/definitions/FileVar"
          }
        }
      }
    },
    "FileAfterRun": {
      "description": "low-level, unvalidated `AfterRun` data exactly how it is stored in the config file",
      "type": "object",
      "properties": {
        "indicatorLines": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "minimum": 0.0
        },
        "newlines": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "minimum": 0.0
        },
        "printResult": {
          "type": [
            "boolean",
            "null"
          ]
        }
      }
    },
    "FileBeforeRun": {
      "description": "low-level, unvalidated `BeforeRun` data exactly how it is stored in the config file",
      "type": "object",
      "properties": {
        "clearScreen": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "newlines": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "minimum": 0.0
        }
      }
    },
    "FileOptions": {
      "description": "low-level, unvalidated `Options` data exactly how it is stored in the config file",
      "type": "object",
      "properties": {
        "afterRun": {
          "anyOf": [
            {
              "$ref": "#/definitions/FileAfterRun"
            },
            {
              "type": "null"
            }
          ]
        },
        "beforeRun": {
          "anyOf": [
            {
              "$ref": "#/definitions/FileBeforeRun"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "FileVar": {
      "description": "an additional variable that gets derived from the file content",
      "type": "object",
      "required": [
        "filter",
        "name",
        "source"
      ],
      "properties": {
        "filter": {
          "description": "how the variable gets computed",
          "type": "string"
        },
        "name": {
          "description": "name of the variable, available as \"{{ name }}\" later",
          "type": "string"
        },
        "source": {
          "description": "the location in the file",
          "allOf": [
            {
              "$ref": "#/definitions/VarSource"
            }
          ]
        }
      }
    },
    "VarSource": {
      "type": "string",
      "enum": [
        "file",
        "line",
        "currentOrAboveLineContent"
      ]
    }
  }
}